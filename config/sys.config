[
 {kernel,
  [{logger_level, all},
   {logger,
    [{handler, otel, otel_log_handler, #{}}]}]},

 {opentelemetry,
  [{processors, [{otel_batch_processor,
                  #{exporter => {otel_exporter_stdout, []}}
                 }]
   }]}
].
